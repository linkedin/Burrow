version: '1'
service:
  name: burrow
  owner: devops
  containers:
    web:
      dockerfile: Dockerfile
      needs:
        - kafka
      secret_files:
        burrow: /etc/burrow/
      ports:
      - name: http
        container: 8000
